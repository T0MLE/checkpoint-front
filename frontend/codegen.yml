overwrite: true
schema: "http://localhost:4000"
documents: "**/*.{ts,tsx}"
generates:
  src/graphql/schema.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
